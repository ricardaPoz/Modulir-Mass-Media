<Window x:Class="Modulir_Mass_Media.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Modulir_Mass_Media" 
        xmlns:local1="clr-namespace:Modulir_Mass_Media.Classes" 
        xmlns:local2="clr-namespace:Modulir_Mass_Media.Helpers"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="882" WindowStartupLocation="CenterScreen">
    <Window.Resources>

        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" SnapsToDevicePixels="true" StrokeThickness="1" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <local2:ClippingBorder x:Key="RadiusImage" CornerRadius="4">
            <Image Stretch="UniformToFill"/>
        </local2:ClippingBorder>




        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="ButtomReadMore" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="5" x:Name="border" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="ListBox.Static.Background" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ListBox.Static.Border" Color="#FFABADB3"/>
        <SolidColorBrush x:Key="ListBox.Disabled.Background" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ListBox.Disabled.Border" Color="#FFD9D9D9"/>
        <Style x:Key="SansBold">
            <Setter Property="TextElement.FontFamily" Value="icons\PTSans-Italic.ttf"/>
        </Style>
        <Style x:Key="SansRegular">
            <Setter Property="TextElement.FontFamily" Value="\icons\ptsans-regular.ttf"/>
        </Style>
    </Window.Resources>
    <Window.DataContext>
        <local1:ModulationMediaWork/>
    </Window.DataContext>
    <Grid>
        <TabControl TabStripPlacement="Top" BorderThickness="0">
            <TabItem>
                <TabItem.Header>
                    <TextBlock FontSize="17">Все новости</TextBlock>
                </TabItem.Header>
                <TabItem.Content>
                    <ListView Background="#F5F5F5" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemsSource="{Binding MassMediaInformationProducts}">
                        <ListView.ItemTemplate  >
                            <DataTemplate>
                                <Border Background="White" CornerRadius="5" Margin="10">
                                    <Grid Background="#FFFFFF" Margin="10 10 0 10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2.5*"/>
                                            <ColumnDefinition Width="7.5*"/>
                                        </Grid.ColumnDefinitions>
                                        <Image  Margin="10 10 10 10" Source="https://icdn.lenta.ru/images/2021/05/09/05/20210509050746907/pic_a52ea4d555a482c55c87acd0a5bcaff0.jpg"/>
                                        <Grid Height="auto" Grid.Column="1" Margin="0 0 0 10">
                                            <Grid.RowDefinitions>
                                                <RowDefinition/>
                                                <RowDefinition/>
                                                <RowDefinition/>
                                                <RowDefinition/>
                                            </Grid.RowDefinitions>
                                            <Grid Grid.Row="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="1.5*"/>
                                                    <ColumnDefinition Width="8.5*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" VerticalAlignment="Center" TextWrapping="Wrap" FontSize="16"  Text="{Binding NameMassMedia}" />
                                                <TextBlock Grid.Column="1" Foreground="#818181" FontSize="16" TextAlignment="Justify" VerticalAlignment="Center"  Grid.Row="0"  Text="{Binding DatePublication}"/>
                                            </Grid>
                                            <TextBlock Style="{DynamicResource SansBold}" Height="auto" TextWrapping="Wrap" FontWeight="Bold" FontSize="23" Grid.Row="1" Text="{Binding InformationProduct.TitleProduct}"/>
                                            <TextBlock Style="{DynamicResource SansRegular}" Height="auto" TextWrapping="Wrap" FontSize="17" Grid.Row="2" Text="{Binding InformationProduct.ContentProduct}"/>
                                            <Button Width="300" HorizontalAlignment="Left" Height="auto" Style="{DynamicResource ButtomReadMore}" FontWeight="Bold" Background="#30A53B" Foreground="White" BorderThickness="0" FontSize="17" Grid.Row="3">Читать подробнее</Button>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>

                        </ListView.ItemTemplate>
                    </ListView>

                </TabItem.Content>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Подписки" FontSize="17"/>
                    </StackPanel>
                </TabItem.Header>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
<!--<Border Grid.Row="" Width="Auto" Height="Auto" BorderBrush="Aqua" BorderThickness="1" CornerRadius="5,5,5,5"/>-->

<!--<Image Grid.Column Source="pack://application:,,,/Modulir Mass Media;component/Icons/Subscription.png"/>-->
    
